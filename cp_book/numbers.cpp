#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);

  int a; // 32 bits number -> -2^31 a 2^31
  long long b; // 64 bits number -> -2^63 a 2^63
  double c; // 64 bits
  long double d; // 80 bits
}
